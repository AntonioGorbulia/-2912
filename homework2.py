class Cat:
    def __init__(self, name, large, color):
        self.name = name
        self.large = large
        self.color = color

    def doing meow(self):
        print(f"Cat {self.name} doing meow")

     def go(self):
        print(f"Cat {self.name} go")

    c = ("Barsik", 100, "grey")

    c.doing meow()
    c.go()
